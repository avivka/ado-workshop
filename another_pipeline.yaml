# ...existing trigger, pool, and steps...
steps:
  # ...existing steps...
    
  - script: |
      pytest --cov=myproject --cov-report=xml
    displayName: 'Run code coverage'
    condition: always()
    
  - task: PublishCodeCoverageResults@1
    inputs:
      codeCoverageTool: Cobertura
      summaryFileLocation: coverage.xml
    condition: always()
